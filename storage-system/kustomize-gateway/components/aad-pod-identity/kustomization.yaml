apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - role.yaml
  - role-binding.yaml
  - service.yaml
  - aad-refresher.yaml

configMapGenerator:
  - name: aad-refresher-script
    files:
      - script.sh

generatorOptions:
  disableNameSuffixHash: true
